# Done

Completed handoffs land here. When an agent finishes its track **and `bun test` is fully green**, it moves its handoff MD from `handoff/` into `handoff/done/`. An empty `handoff/` (besides `README.md`, `CLAUDE.md`, and this folder) means every track is complete.
